version: "3.9"
services:
  dashboard:
    build: .
    ports:
      - "8501:8501"
    environment:
      - ENVIRONMENT=development
    volumes:
      - ./src:/app/src:ro
      - ./data:/app/data
    restart: unless-stopped
